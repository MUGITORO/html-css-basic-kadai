const btn = document.getElementById('btn');
const text = document.getElementById('text');

btn.addEventListener('click', function() {

      // 2秒（2000ミリ秒）後に実行
      setTimeout(function("text").textContent = "ボタンをクリックしました";
      }, 2000);
});